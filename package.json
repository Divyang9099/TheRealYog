{
  "name": "therealyoga-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "start": "npm run start --workspace=backend",
    "build": "npm run build --workspace=frontend",
    "install-all": "npm install"
  },
  "dependencies": {
    "cloudinary": "^2.10.0",
    "glob": "^13.0.6"
  }
}
